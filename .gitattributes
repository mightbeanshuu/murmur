# PDFs are binary artifacts; do not attempt textual whitespace diffs.
*.pdf binary

# The generated handbook uses Markdown's two-space hard line breaks.
Murmur_Placement_Handbook.md whitespace=-trailing-space
